name: code_journey_academy

networks:
  internal_net:
    driver: bridge
    internal: false

services:
  postgres:
    image: postgres:15.6-alpine
    container_name: code_journey_postgres
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-code_journey_db}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres_secure_password_123}
    # Notice: NO ports section to prevent public exposure
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-code_journey_db}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7.2-alpine
    container_name: code_journey_redis
    restart: always
    # Notice: NO ports section to prevent public exposure
    volumes:
      - redis_data:/data
    networks:
      - internal_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: code_journey_backend
    restart: always
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-production}
      SECRET_KEY: ${SECRET_KEY}
      CSRF_SECRET: ${CSRF_SECRET}
      COOKIE_DOMAIN: ${COOKIE_DOMAIN:-codejourneyacademy.com}
      SECURE_COOKIES: ${SECURE_COOKIES:-true}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS}
      DATABASE_URL: ${DATABASE_URL}
      SYNC_DATABASE_URL: ${SYNC_DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      ALLOW_LOCAL_RUNNER_IN_PROD: ${ALLOW_LOCAL_RUNNER_IN_PROD:-false}
    ports:
      - "127.0.0.1:8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - backend_uploads:/app/uploads
    networks:
      - internal_net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://127.0.0.1:8000/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3

  execution-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: code_journey_execution_worker
    restart: always
    command: ["python", "app/worker.py"]
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-production}
      SECRET_KEY: ${SECRET_KEY}
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      ALLOW_LOCAL_RUNNER_IN_PROD: ${ALLOW_LOCAL_RUNNER_IN_PROD:-false}
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - internal_net

  backup-worker:
    image: postgres:15.6-alpine
    container_name: code_journey_backup_worker
    restart: always
    environment:
      PGHOST: postgres
      PGUSER: ${POSTGRES_USER:-postgres}
      PGPASSWORD: ${POSTGRES_PASSWORD:-postgres_secure_password_123}
      PGDATABASE: ${POSTGRES_DB:-code_journey_db}
    entrypoint: ["sh", "-c", "while true; do sleep 86400; pg_dump -U $$PGUSER $$PGDATABASE > /backups/backup_$$(date +%Y%m%d_%H%M%S).sql; done"]
    volumes:
      - backups_data:/backups
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal_net

volumes:
  postgres_data:
  redis_data:
  backend_uploads:
  backups_data:
