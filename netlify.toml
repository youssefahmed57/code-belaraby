# Deprecated: Netlify is not an authoritative staging or production target for authenticated deployments.
# Keep only for legacy static-preview imports. Do not use this file for real platform environments.
[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
